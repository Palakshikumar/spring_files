package com.example.Cart_Shopping.Repository;

import com.example.Cart_Shopping.Model.Address_id;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Adress_Repo extends JpaRepository<Address_id,Integer> {
}
